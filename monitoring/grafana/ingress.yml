---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: grafana
spec:
  parentRefs: [{ name: traefik-gateway, namespace: traefik }]
  hostnames: [admire.mon.enorme.uk]
  rules:
    - backendRefs:
       - name: grafana
         port: 3000
