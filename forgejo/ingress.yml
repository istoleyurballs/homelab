---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: forgejo
spec:
  parentRefs: [{ name: traefik-gateway, namespace: traefik }]
  hostnames: [push.mon.enorme.uk]
  rules:
    - backendRefs:
       - name: forgejo
         port: 3000
