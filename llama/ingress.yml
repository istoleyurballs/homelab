---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: llama
spec:
  parentRefs: [{ name: traefik-gateway, namespace: traefik }]
  hostnames: [slop.mon.enorme.uk]
  rules:
    - backendRefs:
       - name: llama
         port: 8080
