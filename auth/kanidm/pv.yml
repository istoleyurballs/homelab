---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: kanidm-data-pv
spec:
  local:
    path: /mnt/rootfs/@apps/@kanidm/data
  capacity: { storage: 1G }
  accessModes: [ReadWriteOncePod]
  storageClassName: ''
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values: [coruscant]
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kanidm-data-pvc
spec:
  volumeName: kanidm-data-pv
  storageClassName: ''
  accessModes: [ReadWriteOncePod]
  resources: { requests: { storage: 1G } }
