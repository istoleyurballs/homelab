---
apiVersion: v1
kind: Service
metadata:
  name: kanidm
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: kanidm
  ports:
    - name: https
      port: 8443
      protocol: TCP
      targetPort: https
